[project]
name = "weber-formula-verifier"
version = "1.3.0"
description = "Independent SymPy verification of the formulas in Computational-Weber-Electrodynamics.tex"
requires-python = ">=3.10"
dependencies = ["sympy>=1.12"]

[tool.uv]
package = false
