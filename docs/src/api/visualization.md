# Visualization

Functions in this section are available only when the corresponding
optional package is loaded alongside `WeberElectrodynamics`.

## Plots extension (`using Plots`)

```@docs
plot_trajectories
plot_energy
plot_pair_energy
plot_energy_errors
plot_pair_forces
plot_phase_space
plot_momentum_errors
```

## Makie extension (`using GLMakie` / `using CairoMakie`)

```@docs
animate_weber
```
