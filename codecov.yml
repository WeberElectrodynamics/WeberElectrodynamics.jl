coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        if_ci_failed: ignore
    patch:
      default:
        target: 80%
        threshold: 5%
        if_ci_failed: ignore

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
